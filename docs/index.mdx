---
title: Overview
description: See which GitHub App permissions each tested behavior actually uses.
---

GitHub shows what your app is allowed to access. GrantTrace records which
permissions each tested behavior actually exercises, then saves that evidence
in a reviewable `granttrace.lock.json` file.

<Callout type="info" title="Guarantee boundary">
  GrantTrace reports the permissions those scenarios demonstrably require. It
  does not claim whole-application least privilege; untested paths, GraphQL,
  clients outside the supported recording path, and behavior outside recorded
  scenarios stay outside the result.
</Callout>

## Install the public beta

```bash title="Terminal"
npm install --save-dev granttrace@beta
```

GrantTrace is available on
[npm](https://www.npmjs.com/package/granttrace). pnpm users can run
`pnpm add --save-dev granttrace@beta` instead.

## Workflow

```text
install once → run a scenario through GrantTrace → review its permission diff
→ commit granttrace.lock.json → check it in CI → optionally prove live
```

## Start

<Cards>
  <Card
    title="Quickstart"
    description="Install GrantTrace, wrap one real test, and commit the permission receipt. Starts from zero."
    href="/docs/getting-started"
  />
  <Card
    title="Custom transports"
    description="Use explicit instrumentation only when automatic recording cannot observe your client."
    href="/docs/instrument-octokit"
  />
  <Card
    title="How it works"
    description="Understand scenarios, evidence, solving, and the claim boundary."
    href="/docs/how-it-works"
  />
  <Card
    title="Engineering case study"
    description="Explore the architecture, security decisions, and deliberate tradeoffs."
    href="/docs/case-study"
  />
  <Card
    title="Measured assurance"
    description="Reproduce oracle checks, coverage floors, and bounded local benchmarks."
    href="/docs/assurance"
  />
  <Card
    title="CLI"
    description="Find commands, flags, timeouts, and exit codes."
    href="/docs/cli-reference"
  />
</Cards>

## See a complete consumer

The
[triage-bot example](https://github.com/ToxFied/GrantTrace/tree/main/examples/triage-bot)
contains application code, a test, a hermetic scenario, expected review
output, and the resulting committed contract.
